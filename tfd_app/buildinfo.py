# -*- coding: utf-8 -*-
"""构建信息 · **版本号唯一来源**
==================================================================
新界面（``tfd_app/ui2/``）与旧界面（``tfd_app/gui.py``）都从这里取版本号。

为什么要有这个文件：新界面视图层原本把版本号**写死**在页脚文案里，
后果有二 —— ① 发布 bump 后界面不跟着变；② 两仓共用该文件，**导师版会显示
学生版的版本号**。改成唯一来源后，页脚文案由 ``APP_VERSION`` 渲染。

发布时改**两处**即可：本文件的 ``APP_VERSION`` + 仓库根 ``VERSION`` 文件；
``tests/test_assets.py`` 会断言两者一致（含界面源码里不得再出现写死版本号）。
"""
APP_VERSION = "1.3.124"
